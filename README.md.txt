The project is only using JavaScript to manipulate the DOM.   I created an index.html since you need this 
with the DOM and then just changed styling with js. 
I also created a funciton so that my images change when the button is clicked.
